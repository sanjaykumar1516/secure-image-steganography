// script.js
// Step 1 placeholder. Client-side capacity preview, AJAX form submission,
// and dashboard rendering will be added starting Step 6 (Frontend) and
// extended in Step 10 (Experimental graphs).

document.addEventListener("DOMContentLoaded", () => {
  console.log("Secure Image Steganography — UI loaded (Step 1 skeleton).");
});